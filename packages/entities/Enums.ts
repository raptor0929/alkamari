export enum OrderType{
    Buy = 0,
    Sell = 1
}

export enum Currency{
    USDT = 0,
    BOB = 1
} 

export enum OrderStatus{
    Initialized = 0,
    InProgress = 1,
    Completed = 2,
    Rejected = 3
}